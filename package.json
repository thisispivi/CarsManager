{
  "name": "carsmanager",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for CarsManager monorepo tooling (commit linting, pre-commit hooks).",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0"
  }
}
